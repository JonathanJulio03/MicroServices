server:
  port:8081

sping:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JonathanJulio03/MicroServices.git
          searchPath: ConfigData
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
      
